{
  "name": "arl-openrouter-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "./deploy.sh deploy"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.92.0"
  }
}
